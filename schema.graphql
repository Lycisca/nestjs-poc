type User {
  id: ID!
  firstName: String
  lastName: String
}

type Query {
  user(id: ID!): User
}

input CreateUserInput {
  firstName: String!
  lastName: String
}

type Mutation {
  createUser(input: CreateUserInput): User
}
